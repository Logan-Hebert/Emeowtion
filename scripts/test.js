// This will run on index.html when it is opened
const myButton = document.getElementById('myButton')

// You can now add an event listener to it
myButton.addEventListener('click', () => {
    console.log('Button was clicked!');
});
